# Gateway stubs for harness_x — lightweight replacements for the full gateway module.
# The gateway (messaging platform adapters) has been removed; these stubs provide
# the minimal API surface that the retained agent/tools code depends on.
